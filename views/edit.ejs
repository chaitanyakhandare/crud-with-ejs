<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body class="bg-zinc-900 text-white">
    <div class="w-full p-10 space-y-5">

        <div class="flex flex-col flex-wrap mb-10">
            <a class="text-blue-500 hover:text-blue-700" href="/">
                < Back to Home </a>
                    <a class="text-blue-500 hover:text-blue-700" href="/read">
                        < Back to All Profiles </a>
        </div>

        <h1 class="text-3xl tracking-tighter">
            Updating - <span class="text-yellow-500">
                <%= user.name %>
            </span>
        </h1>
        <div class="users flex flex-wrap gap-5">

            <div class="user p-4 w-64 rounded-lg bg-zinc-800 space-y-5">
                <div class="w-full h-64 rounded-lg bg-zinc-700 overflow-hidden">
                    <img class="w-full h-full object-cover object-center" src="<%= user.image %>" alt="">
                </div>
                <div>
                    <h3 class="text-xl tracking-tighter">
                        <%= user.name %>
                    </h3>
                    <p class="text-sm text-zinc-500">
                        <%= user.email %>
                    </p>
                </div>
            </div>

            <form action="/update/<%= user._id %>" method="post" class="gap-3 flex flex-col flex-wrap">
                <input class="px-5 py-3 bg-transparent border-2 border-zinc-800 outline-none rounded-lg" name="newname"
                    placeholder="New Name" type="text" required>
                <input class="px-5 py-3 bg-transparent border-2 border-zinc-800 outline-none rounded-lg" name="newemail"
                    placeholder="New Email Address" type="email" required>
                <input class="px-5 py-3 bg-transparent border-2 border-zinc-800 outline-none rounded-lg" name="newimage"
                    placeholder="New Image URL" value="<%= user.image %>" type="text" required>
                <button class="px-5 py-3 bg-yellow-500 text-black rounded-lg hover:bg-yellow-600" type="submit">Update
                    <%= user.name %></button>
            </form>

        </div>

    </div>
</body>

</html>